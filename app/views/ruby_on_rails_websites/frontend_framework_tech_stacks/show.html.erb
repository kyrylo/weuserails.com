<%
  title = "rails web apps using the #{@frontend_framework.name.downcase} frontend framework"
  description = "A categorized list of Ruby on Rails web apps using the #{@frontend_framework.name} frontend framework."

  set_meta_tags(
    title: title,
    description: description,
    canonical: frontend_framework_tech_stack_url(@frontend_framework.slug),
    og: {
      title: title,
      description: description,
    },
    twitter: {
      title: title,
      description: description,
    },
    prev: @prev_page ? frontend_framework_tech_stack_url(@frontend_framework.slug, page: @current_page - 1) : nil,
    next: @next_page ? frontend_framework_tech_stack_url(@frontend_framework.slug, page: @current_page + 1) : nil
  )
%>

<article class="article">
  <%= render "shared/sites_by_technology", sites: @sites %>

  <div class="mt-2 pagination">
    <% if @prev_page %>
      <%= link_to "◂ prev", frontend_framework_tech_stack_path(page: @current_page - 1), class: "no-visited" %>
    <% end %>
    <% if @next_page %>
      <%= link_to "next ▸", frontend_framework_tech_stack_path(page: @current_page + 1), class: "no-visited" %>
    <% end %>
  </div>
</article>
